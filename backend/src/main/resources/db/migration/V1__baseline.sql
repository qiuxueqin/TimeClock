-- S0-DB-01 基线迁移
-- 规则：UTF-8 utf8mb4；字段命名 snake_case；主键 UUID 用 CHAR(36)；
-- 审计字段：created_at / updated_at（DATETIME(6) 带时区无关的时间点）。
-- 迁移不可改写；后续只允许追加修正迁移。
-- 连接信息仅通过环境变量注入；本文件不含任何凭据。

-- 本基线为空骨架。后续数据步骤（S1-DB-01 起）逐个追加建表迁移。
-- 说明：Flyway 基线迁移（V1__baseline.sql）允许为空，用于标记迁移起点，
-- 使后续 V2/V3... 迁移从空库顺序执行并可回滚预案审阅。

SELECT 1;
